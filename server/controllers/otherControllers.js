export const setContactQuery = async (req, res) => {
    try {
        const { locAuthEmail = null } = req.body
        if (locAuthEmail !== null) {
            
        }
        
    } catch (error) {

    }
}